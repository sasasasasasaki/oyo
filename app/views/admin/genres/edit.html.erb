　　<div class="container">
　　  
　　<div class="col-md-3">
　　<h2 class="shadow- p-3 mb-5 bg-light">ジャンル編集</h2>
　　 </div>
　　    <%= form_with model: @genre,url: admin_genre_path do |f| %>
      <table>
        <tr>
          <th>ジャンル名</th>
          <th><%= f.text_field :name %></th>
          <th><%= f.submit '変更を保存',class:"btn btn-success"%></th>
        </tr>
      </table>
      
　　<% end %>