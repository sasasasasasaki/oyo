<h2><%= @order.customer.first_name %><%= @order.customer.last_name %>さんの注文履歴</h2>

 <table>

   <tr>
      <th>購入日時</th>
      <th>注文個数</th>
   </tr>

   <tr>
     <td><%= @order.created_at.strftime('%Y/%m/%d %H:%M') %></td>

  <% @order.order_details.each do |order_detail| %>
     <td><%= order_detail.amount %></td>
  <% end %>

   </tr>


 </table>
